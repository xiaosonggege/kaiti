信号分析流程：
1.信号去噪
2.
(1)传统办法：去基线，寻峰(峰值识别)，计算峰值面积，根据峰值面积查表得到对应元素(粒子识别，本地识别)
(2)现在的办法：
1. 去基线
2.本底甄别：训练时feature是信号，label是000001111110000....这样的编码，预测时将一整段信号拆分成一些段，
然后做预测，得到每段的0000111这样的信号，然后拼接即可得到
3.峰值识别：训练时feature是信号，label是信号，但信号中峰值区域起点和终点要变形，起点位置信号值直接变为很小的值，终点信号直接变成很大的值，
或者起点和终点信号值范围分别小于min(signal),大于max(signal)